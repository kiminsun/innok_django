# 프로덕션 환경 의존성
Django>=4.2.0
django-bootstrap5>=24.0
Pillow>=10.0.0

# AWS 관련
django-storages>=1.14.0
boto3>=1.28.0

# 프로덕션 서버
gunicorn>=21.2.0
psycopg2-binary>=2.9.0  # PostgreSQL 사용 시
mysqlclient>=2.2.0  # MySQL 사용 시

# 보안
python-decouple>=3.8  # 환경 변수 관리

